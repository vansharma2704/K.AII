"use client";

import React from "react";

const DashboardBackground = () => {
  return (
    <group>
      {/* 
          Dashboard now uses the GlobalCosmicEffects (galaxy, stars, nebula) 
          for a consistent, high-fidelity experience across the platform.
      */}
    </group>
  );
};

export default DashboardBackground;
